package vector;

import java.util.Enumeration;
import java.util.Iterator;
import java.util.ListIterator;
import java.util.Spliterator;
import java.util.Spliterators;
import java.util.Vector;

public class iterator {

	public static void main(String[] args) {
	      Vector<Integer> integers=new Vector<>();
	      integers.add(12);
	      integers.add(14);
	      integers.add(15);
          Spliterator<Integer> spliterators=integers.spliterator();
          spliterators.forEachRemaining(num->System.out.println(num));
         Enumeration<Integer> iEnumeration=integers.elements();
         while(iEnumeration.hasMoreElements())
         {
        	 System.out.println(iEnumeration.nextElement());
         }
         while(iEnumeration.hasMoreElements())
         {
        	 System.out.println(iEnumeration.nextElement());
         }while(iEnumeration.hasMoreElements())
         {
        	 System.out.println(iEnumeration.nextElement());
         }
         
         while(iEnumeration.hasMoreElements())
         {
        	 System.out.println(iEnumeration.nextElement());
         }
         while(iEnumeration.hasMoreElements())
         {
        	 System.out.println(iEnumeration.nextElement());
         }
         while(iEnumeration.hasMoreElements())
         {
        	 System.out.println(iEnumeration.nextElement());
         }
         while(iEnumeration.hasMoreElements())
         {
        	 System.out.println(iEnumeration.nextElement());
         }
         while(iEnumeration.hasMoreElements())
         {
        	 System.out.println(iEnumeration.nextElement());
         }
         while(iEnumeration.hasMoreElements())
         {
        	 System.out.println(iEnumeration.nextElement());
         }
         while(iEnumeration.hasMoreElements())
         {
        	 System.out.println(iEnumeration.nextElement());
         }
         while(iEnumeration.hasMoreElements())
         {
        	 System.out.println(iEnumeration.nextElement());
         }
         while(iEnumeration.hasMoreElements())
         {
        	 System.out.println(iEnumeration.nextElement());
         }
         while(iEnumeration.hasMoreElements())
         {
        	 System.out.println(iEnumeration.nextElement());
         }
         while(iEnumeration.hasMoreElements())
         {
        	 System.out.println(iEnumeration.nextElement());
         }
         while(iEnumeration.hasMoreElements())
         {
        	 System.out.println(iEnumeration.nextElement());
         }
         while(iEnumeration.hasMoreElements())
         {
        	 System.out.println(iEnumeration.nextElement());
         }
         
         while(iEnumeration.hasMoreElements())
         {
        	 System.out.println(iEnumeration.nextElement());
         }while(iEnumeration.hasMoreElements())
         {
        	 System.out.println(iEnumeration.nextElement());
         }
         
         while(iEnumeration.hasMoreElements())
         {
        	 System.out.println(iEnumeration.nextElement());
         }
         while(iEnumeration.hasMoreElements())
         {
        	 System.out.println(iEnumeration.nextElement());
         }
         while(iEnumeration.hasMoreElements())
         {
        	 System.out.println(iEnumeration.nextElement());
         }
         while(iEnumeration.hasMoreElements())
         {
        	 System.out.println(iEnumeration.nextElement());
         }
         while(iEnumeration.hasMoreElements())
         {
        	 System.out.println(iEnumeration.nextElement());
         }
         while(iEnumeration.hasMoreElements())
         {
        	 System.out.println(iEnumeration.nextElement());
         }
          
	}

}
